#!/bin/bash
# This script creates a Fuse.js index for searching through notes.
# It requires Node.js and ts-node to run the TypeScript script.
# Start in the workspace root directory.
ts-node src/script/create-fuse-index.ts